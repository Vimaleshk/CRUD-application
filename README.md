# CRUD-application
This project is a CRUD (Create, Read, Update, Delete) API built using Express.js and MongoDB. It demonstrates how to design and implement RESTful APIs for managing data in a MongoDB database. The application provides endpoints to perform basic operations on a resource.
